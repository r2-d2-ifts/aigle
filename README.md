
  # Generate UI from Markdown

  This is a code bundle for Generate UI from Markdown. The original project is available at https://www.figma.com/design/wRPCHutX4rq9ffrzrHHTlA/Generate-UI-from-Markdown.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  